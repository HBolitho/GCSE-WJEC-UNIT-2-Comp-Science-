CREATE TABLE xxxxxxx.stalls( # Place your database name in place for the xxxx
bookingid INT PRIMARY KEY auto_increment,
companyname varchar(50),
datestart date,
dateend date,
size varchar(10),
facilities varchar(25),
people varchar (5),
location varchar (10)
);

SELECT * FROM xxxxxxxxxx.stalls  # Brings up the table onto your SQL